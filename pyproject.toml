[project]
name = "weathership-site"
version = "0.0.1"
description = "Public web presence and brand package for weathership.org"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27",
]

[dependency-groups]
dev = [
  "behave>=1.2.6",
  "pytest>=8.0",
]
