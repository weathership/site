# Nautilus landing band + project page

Full-viewport-width Supervision band on `/` (after the hero), linking
to `/projects/nautilus/` in the same article layout as the other
project pages. Nautilus is excluded from the landing card grid so it
is not doubled; it still appears on `/projects/`.

Copy from `zndx.supervision.v1` / weathership/nautilus README: agent-free
Rust supervisor, instance-per-project, Brier ledger, Overwatch
escalation. Repo: `https://github.com/weathership/nautilus`.
